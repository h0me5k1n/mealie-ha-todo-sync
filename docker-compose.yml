services:
  sync:
    build: .
    env_file: .env
    # Run once; restart manually or schedule via cron on the host.
    # To run on a schedule inside Docker, uncomment and adjust:
    # command: ["sh", "-c", "while true; do python sync.py; sleep 300; done"]
